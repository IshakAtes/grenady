NODE_ENV=production
PORT=3000
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=mail@example.com
SMTP_PASS=change-me
SMTP_FROM="Grenady <mail@grenady.de>"
INQUIRY_RECIPIENT_EMAIL=ishakfeuer@gmail.com
INQUIRY_ALLOWED_ORIGIN=https://grenady.de,https://www.grenady.de

# Local API only, used by `npm run dev:api`.
API_PORT=8787

# Build-time frontend override only. Not needed for the VPS container
# because the API runs in the same container at /api/send-inquiry.
# VITE_INQUIRY_API_URL=/api/send-inquiry
